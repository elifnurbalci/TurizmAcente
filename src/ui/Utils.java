package ui;

public class Utils {
    // Example of a utility method
    public static double calculateTotalPrice(int adults, int children, double pricePerAdult, double pricePerChild) {
        return (adults * pricePerAdult) + (children * pricePerChild);
    }
    // More utility methods as needed for the application
}
